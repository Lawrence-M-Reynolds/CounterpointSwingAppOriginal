# CounterpointSwingAppOriginal
The counterpoint swing app about how it was at submission. Runs on it's own without dependencies.

# Build with the following:
mvn clean install

# Then run with:
java -jar target/CounterpointSwingAppOriginal.jar
